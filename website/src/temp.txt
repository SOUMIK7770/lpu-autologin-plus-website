import { useEffect, useState } from "react";
import { motion, useScroll, useTransform } from "framer-motion";

// 👉 import your QR image here
import qrUPI from "./assets/upi-qr.png"; // make sure name matches your file

const sections = [
  { id: "hero", label: "Home" },
  { id: "about", label: "About" },
  { id: "download", label: "Download" },
  { id: "howto", label: "How to Setup" },
  { id: "donation", label: "Donate" },
  { id: "support", label: "Support" },
  { id: "feedback", label: "Feedback" },
];

const fadeInUp = {
  hidden: { opacity: 0, y: 30 },
  visible: (delay = 0) => ({
    opacity: 1,
    y: 0,
    transition: { duration: 0.6, delay, ease: "easeOut" },
  }),
};

function App() {
  const [activeSection, setActiveSection] = useState("hero");
  const { scrollYProgress } = useScroll();
  const progressWidth = useTransform(scrollYProgress, [0, 1], ["0%", "100%"]);

  useEffect(() => {
    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            setActiveSection(entry.target.id);
          }
        });
      },
      { threshold: 0.4 }
    );

    sections.forEach((s) => {
      const el = document.getElementById(s.id);
      if (el) observer.observe(el);
    });

    return () => observer.disconnect();
  }, []);

  const scrollToSection = (id) => {
    const el = document.getElementById(id);
    if (!el) return;
    el.scrollIntoView({ behavior: "smooth", block: "start" });
  };

  return (
    <div className="min-h-screen bg-darkBg text-slate-50 font-sans relative overflow-x-hidden">
      {/* Animated gradient background */}
      <div className="pointer-events-none fixed inset-0 -z-10">
        <div className="absolute -top-40 -left-32 h-80 w-80 rounded-full bg-primary/30 blur-3xl" />
        <div className="absolute top-40 right-0 h-96 w-96 rounded-full bg-secondary/25 blur-3xl" />
        <div className="absolute bottom-0 left-1/3 h-72 w-72 rounded-full bg-purple-500/20 blur-3xl" />
      </div>

      {/* Scroll progress bar */}
      <motion.div
        style={{ width: progressWidth }}
        className="fixed top-0 left-0 h-1 bg-gradient-to-r from-primary via-secondary to-purple-500 z-40"
      />

      {/* Navbar */}
      <header className="sticky top-0 z-30 backdrop-blur-xl bg-slate-900/40 border-b border-white/5">
        <div className="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8 py-3 flex items-center justify-between">
          <div
            className="flex items-center gap-2 cursor-pointer"
            onClick={() => scrollToSection("hero")}
          >
            <div className="h-8 w-8 rounded-xl bg-gradient-to-br from-primary to-secondary flex items-center justify-center text-xs font-bold shadow-soft">
              L+
            </div>
            <div className="leading-tight">
              <p className="text-sm font-semibold">LPU AutoLogin+</p>
              <p className="text-[11px] text-slate-400">
                Wi-Fi Auto Login Extension
              </p>
            </div>
          </div>

          <nav className="hidden md:flex items-center gap-3 text-xs">
            {sections.map((s) => (
              <button
                key={s.id}
                onClick={() => scrollToSection(s.id)}
                className={`px-3 py-1 rounded-full transition ${
                  activeSection === s.id
                    ? "bg-white/10 text-white shadow-soft"
                    : "text-slate-300 hover:bg-white/5"
                }`}
              >
                {s.label}
              </button>
            ))}
          </nav>
        </div>
      </header>

      {/* Main content */}
      <main className="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8 pb-16 space-y-32 md:space-y-40">
        {/* Hero / Extension showcase */}
        <section
          id="hero"
          className="pt-12 sm:pt-16 md:pt-24 grid md:grid-cols-2 gap-10 items-center max-w-full"
        >
          <motion.div
            initial="hidden"
            animate="visible"
            variants={fadeInUp}
            className="space-y-6"
          >
            <span className="inline-flex items-center gap-2 rounded-full bg-white/5 px-3 py-1 text-xs text-slate-300 border border-white/10">
              <span className="h-2 w-2 rounded-full bg-emerald-400 animate-pulse" />
              Built for LPU 24Online Wi-Fi
            </span>

            <h1 className="text-4xl md:text-5xl lg:text-6xl font-semibold leading-tight">
              Auto-login to{" "}
              <span className="bg-gradient-to-r from-primary via-secondary to-purple-400 bg-clip-text text-transparent">
                LPU Wi-Fi
              </span>{" "}
              every time.
            </h1>

            <p className="text-base md:text-lg text-slate-300 max-w-xl">
              LPU AutoLogin+ is a lightweight open-source browser extension that
              automatically fills your ID, password, agrees to the Terms &
              Conditions, and logs you into the LPU internet portal within a
              second.
            </p>

            <div className="flex flex-wrap gap-3">
              <button
                onClick={() => scrollToSection("download")}
                className="px-5 py-2.5 rounded-xl bg-gradient-to-r from-primary to-secondary text-sm font-medium shadow-soft hover:scale-[1.02] active:scale-[0.99] transition"
              >
                Download Extension
              </button>
              <button
                onClick={() => scrollToSection("howto")}
                className="px-4 py-2.5 rounded-xl border border-white/10 text-sm text-slate-200 hover:bg-white/5 transition"
              >
                How it works
              </button>
            </div>

            <div className="flex flex-wrap gap-4 text-xs sm:text-sm text-slate-400">
              <span>⚡ One-time setup</span>
              <span>🔒 Runs locally only</span>
              <span>🧑‍💻 Built by LPU CSE student</span>
            </div>
          </motion.div>

          <motion.div
            className="relative flex justify-center md:justify-end"
            initial={{ opacity: 0, x: 40 }}
            animate={{ opacity: 1, x: 0 }}
            transition={{ duration: 0.7, ease: "easeOut" }}
          >
            <div className="absolute -inset-1 rounded-3xl bg-gradient-to-br from-primary/40 via-purple-500/20 to-secondary/40 blur-2xl opacity-70" />
            <div className="relative rounded-3xl bg-card border border-white/10 p-4 md:p-6 shadow-soft w-full max-w-[440px]">
              {/* Fake browser frame */}
              <div className="flex items-center justify-between mb-4">
                <div className="flex gap-1.5">
                  <span className="h-2.5 w-2.5 rounded-full bg-red-400/80" />
                  <span className="h-2.5 w-2.5 rounded-full bg-yellow-400/80" />
                  <span className="h-2.5 w-2.5 rounded-full bg-emerald-400/80" />
                </div>
                <div className="px-3 py-1 rounded-full bg-slate-900/60 text-[10px] text-slate-300">
                  https://internet.lpu.in/24online/...
                </div>
              </div>

              <div className="space-y-4 text-xs sm:text-[13px]">
                <div className="bg-slate-900/60 border border-white/5 rounded-2xl p-3 space-y-3">
                  <div className="space-y-1">
                    <label className="text-[11px] text-slate-400">
                      User Name
                    </label>
                    <div className="h-8 rounded-xl bg-slate-800/90 border border-white/10 flex items-center px-2 text-slate-300 text-[11px]">
                      1240•••••
                    </div>
                  </div>
                  <div className="space-y-1">
                    <label className="text-[11px] text-slate-400">
                      Password
                    </label>
                    <div className="h-8 rounded-xl bg-slate-800/90 border border-white/10 flex items-center px-2 text-slate-500 text-[13px]">
                      ••••••••••
                    </div>
                  </div>
                  <div className="flex items-center justify-between text-[11px]">
                    <div className="flex items-center gap-2">
                      <div className="h-3.5 w-3.5 rounded-[6px] bg-primary flex items-center justify-center">
                        <div className="h-2 w-2 rounded-[4px] bg-white" />
                      </div>
                      <span className="text-slate-300">
                        I agree with Terms &amp; Conditions
                      </span>
                    </div>
                    <button className="px-3 py-1.5 rounded-lg bg-gradient-to-r from-primary to-secondary text-[11px] font-medium shadow-soft">
                      Auto Login
                    </button>
                  </div>
                </div>

                <div className="flex items-center justify-between text-[11px] text-slate-400">
                  <span>
                    Extension status:{" "}
                    <span className="text-emerald-400">Active</span>
                  </span>
                  <span>Login time: &lt; 1 second</span>
                </div>
              </div>
            </div>
          </motion.div>
        </section>

        {/* About Developer */}
        <section id="about" className="space-y-8">
          <motion.h2
            className="text-2xl md:text-3xl font-semibold"
            initial="hidden"
            whileInView="visible"
            viewport={{ once: true }}
            custom={0}
            variants={fadeInUp}
          >
            About the Developer
          </motion.h2>

          <motion.div
            className="grid md:grid-cols-[1.2fr,0.8fr] gap-8 items-center"
            initial="hidden"
            whileInView="visible"
            viewport={{ once: true }}
            variants={fadeInUp}
            custom={0.1}
          >
            <div className="space-y-4 text-sm md:text-base text-slate-300">
              <p>
                Hey! I&apos;m <span className="font-semibold">SOUMIK POHI</span>
                , a CSE student at Lovely Professional University and a tech
                enthusiast obsessed with automating boring tasks.
              </p>
              <p>
                LPU AutoLogin+ was built to solve a simple everyday annoyance:
                manually entering credentials and clicking the same Terms &
                Conditions checkbox every single time just to use the internet.
              </p>
              <p>
                This project is completely open-source and privacy-focused — all
                logic runs locally in your browser. No data is collected or sent
                anywhere.
              </p>
              <p>
                I love working on automation, cybersecurity, and tools that make
                student life smoother. Your feedback and support help me build
                more such utilities.
              </p>
            </div>

            <div className="rounded-3xl bg-card border border-white/10 p-5 space-y-4 shadow-soft">
              <div className="flex items-center gap-3">
                <div className="h-12 w-12 rounded-2xl bg-gradient-to-br from-primary to-secondary flex items-center justify-center text-lg font-bold">
                  S
                </div>
                <div>
                  <p className="text-sm font-semibold">SOUMIK POHI</p>
                  <p className="text-[11px] text-slate-400">
                    B.Tech CSE @ LPU • Open-source builder
                  </p>
                </div>
              </div>

              <div className="text-[12px] text-slate-400 space-y-1">
                <p>Focus areas:</p>
                <ul className="list-disc list-inside">
                  <li>Automation & productivity tools</li>
                  <li>Cybersecurity & scripting</li>
                  <li>Student-focused open-source utilities</li>
                </ul>
              </div>

              <div className="flex gap-3 pt-1 flex-wrap">
                <SocialChip
                  label="GitHub"
                  href="https://github.com/SOUMIK7770"
                />
                <SocialChip label="LinkedIn" href="#" />
                <SocialChip label="Instagram" href="#" />
              </div>
            </div>
          </motion.div>
        </section>

        {/* Download Section */}
        <section id="download" className="space-y-8">
          <motion.h2
            className="text-2xl md:text-3xl font-semibold"
            initial="hidden"
            whileInView="visible"
            viewport={{ once: true }}
            variants={fadeInUp}
          >
            Download LPU AutoLogin+
          </motion.h2>

          <motion.div
            className="grid md:grid-cols-3 gap-6 items-start"
            initial="hidden"
            whileInView="visible"
            viewport={{ once: true }}
            variants={fadeInUp}
            custom={0.1}
          >
            <div className="md:col-span-2 space-y-4 text-sm md:text-base text-slate-300">
              <p>
                Choose your preferred source to download the extension. Both
                contain the same safe, open-source code.
              </p>
              <div className="flex flex-wrap gap-4">
                <a
                  href="https://github.com/SOUMIK7770/LPU-autologin-plus"
                  target="_blank"
                  rel="noreferrer"
                  className="flex-1 min-w-[180px] text-center px-4 py-3 rounded-2xl bg-gradient-to-r from-primary to-secondary text-sm font-medium shadow-soft hover:scale-[1.02] active:scale-[0.99] transition"
                >
                  View on GitHub
                </a>
                <a
                  href="https://drive.google.com/drive/folders/1Be3DOLH87uAvfOAvd2GmWlqK-Le8wpG2?usp=sharing"
                  target="_blank"
                  rel="noreferrer"
                  className="flex-1 min-w-[180px] text-center px-4 py-3 rounded-2xl border border-white/15 bg-white/5 text-sm font-medium hover:bg白/10 transition"
                >
                  Download ZIP (Google Drive)
                </a>
              </div>

              <div className="text-xs text-slate-400 space-y-1">
                <p>
                  Current version:{" "}
                  <span className="text-slate-200">v1.0.0</span>
                </p>
                <p>
                  Changelog: Initial public release with auto-fill, auto Terms
                  &amp; Conditions, and auto-login support for{" "}
                  <span className="font-mono text-[11px]">
                    https://internet.lpu.in/24online/webpages/client.jsp
                  </span>
                  .
                </p>
              </div>
            </div>

            <div className="rounded-3xl bg-card border border-white/10 p-4 text-xs sm:text-sm text-slate-300 space-y-2 shadow-soft">
              <p className="font-semibold text-slate-100">
                Requirements & Compatibility
              </p>
              <ul className="list-disc list-inside space-y-1">
                <li>Chrome / Edge / Brave browser</li>
                <li>Manifest V3 compatible</li>
                <li>Works on desktop & laptop</li>
                <li>Tested on LPU campus network</li>
              </ul>
            </div>
          </motion.div>
        </section>

        {/* How to Setup */}
        <section id="howto" className="space-y-8">
          <motion.h2
            className="text-2xl md:text-3xl font-semibold"
            initial="hidden"
            whileInView="visible"
            viewport={{ once: true }}
            variants={fadeInUp}
          >
            How to Setup the Extension
          </motion.h2>

          <motion.div
            className="grid lg:grid-cols-[1.3fr,0.7fr] gap-8"
            initial="hidden"
            whileInView="visible"
            viewport={{ once: true }}
            variants={fadeInUp}
            custom={0.1}
          >
            <div className="space-y-4">
              <ol className="space-y-3 text-sm md:text-base text-slate-300">
                {[
                  "Download the ZIP file from GitHub or Google Drive.",
                  "Extract the ZIP to a folder on your system.",
                  "Open your browser and go to chrome://extensions.",
                  "Enable Developer Mode (top-right corner).",
                  "Click on 'Load unpacked' and select the extracted folder.",
                  "Make sure the extension toggle is switched ON.",
                  "Connect to LPU Wi-Fi, open the internet portal — and let the extension auto-login for you.",
                ].map((step, idx) => (
                  <li
                    key={idx}
                    className="flex gap-3 items-start bg-card border border-white/10 rounded-2xl p-3"
                  >
                    <div className="flex-shrink-0 h-7 w-7 rounded-xl bg-gradient-to-br from-primary to-secondary flex items-center justify-center text-[11px] font-semibold shadow-soft">
                      {idx + 1}
                    </div>
                    <p className="pt-0.5">{step}</p>
                  </li>
                ))}
              </ol>
            </div>

            <div className="rounded-3xl bg-card border border-white/10 p-4 text-xs sm:text-sm text-slate-300 space-y-3 shadow-soft">
              <p className="font-semibold text-slate-100">Demo video</p>
              <p>
                A short video tutorial showing the full setup and auto-login
                flow will be added here. For now, follow the written steps and
                you&apos;ll be done in under a minute.
              </p>
              <div className="aspect-video rounded-2xl bg-slate-900/70 border border-dashed border-white/20 flex items-center justify-center text-[11px] text-slate-500">
                Demo video placeholder
              </div>
            </div>
          </motion.div>
        </section>

        {/* Donation Section */}
        <section id="donation" className="space-y-8">
          <motion.h2
            className="text-2xl md:text-3xl font-semibold"
            initial="hidden"
            whileInView="visible"
            viewport={{ once: true }}
            variants={fadeInUp}
          >
            Support the Project ❤️
          </motion.h2>

          <motion.div
            className="grid md:grid-cols-[1.2fr,0.8fr] gap-8 items-center"
            initial="hidden"
            whileInView="visible"
            viewport={{ once: true }}
            variants={fadeInUp}
            custom={0.1}
          >
            <div className="space-y-4 text-sm md:text-base text-slate-300">
              <p>
                If LPU AutoLogin+ saves you time every day, consider supporting
                future open-source projects. Even a small contribution motivates
                and helps fund better tools for students.
              </p>
              <p>
                I plan to build more utilities around Wi-Fi automation,
                productivity, and developer-focused tools. Donations go towards
                domain costs, hosting, and snacks during late-night coding
                sessions 😄
              </p>

              <div className="flex flex-wrap gap-3 text-xs sm:text-sm">
                <Pill label="UPI support" />
                <Pill label="No tracking, no analytics" />
                <Pill label="Student-built, student-first" />
              </div>
            </div>

            <div className="rounded-3xl bg-card border border-white/10 p-5 space-y-4 shadow-soft">
              <p className="text-xs sm:text-sm text-slate-300 text-center">
                Scan the QR below to donate (UPI, PayTM, etc.)
              </p>
              <div className="flex items-center justify-center">
                {/* Real QR image */}
                <div className="relative">
                  <div className="absolute inset-0 rounded-2xl bg-primary/20 blur-xl" />
                  <img
                    src={qrUPI}
                    alt="UPI QR Code"
                    className="relative h-40 w-40 rounded-2xl border border-white/15 object-cover shadow-soft"
                  />
                </div>
              </div>
              <p className="text-[11px] text-slate-400 text-center">
                UPI ID:{" "}
                <span className="text-slate-100 font-mono">7873419960@upi</span>
              </p>
            </div>
          </motion.div>
        </section>

        {/* Support & Contact */}
        <section id="support" className="space-y-8">
          <motion.h2
            className="text-2xl md:text-3xl font-semibold"
            initial="hidden"
            whileInView="visible"
            viewport={{ once: true }}
            variants={fadeInUp}
          >
            Support & Contact
          </motion.h2>

          <motion.div
            className="grid md:grid-cols-2 gap-6"
            initial="hidden"
            whileInView="visible"
            viewport={{ once: true }}
            variants={fadeInUp}
            custom={0.1}
          >
            <div className="rounded-3xl bg-card border border-white/10 p-5 space-y-3 shadow-soft text-sm md:text-base text-slate-300">
              <p className="font-semibold text-slate-100">
                Having an issue with the extension?
              </p>
              <p>
                If something doesn&apos;t work as expected — after a Wi-Fi
                update, browser change, or portal change — you can reach out
                directly.
              </p>
              <ul className="list-disc list-inside text-xs sm:text-sm text-slate-400 space-y-1">
                <li>Mention your browser and version</li>
                <li>Share a screenshot of the portal (without password)</li>
                <li>Describe what exactly fails (auto-fill, button, etc.)</li>
              </ul>
            </div>

            <div className="rounded-3xl bg-card border border-white/10 p-5 space-y-4 shadow-soft text-sm md:text-base text-slate-300">
              <p className="font-semibold text-slate-100">Contact channels</p>
              <div className="space-y-2 text-xs sm:text-sm text-slate-400">
                <p>
                  GitHub Issues:{" "}
                  <a
                    href="https://github.com/SOUMIK7770/LPU-autologin-plus/issues"
                    target="_blank"
                    rel="noreferrer"
                    className="text-secondary hover:underline"
                  >
                    Open an issue
                  </a>
                </p>
                <p>
                  GitHub Profile:{" "}
                  <a
                    href="https://github.com/SOUMIK7770"
                    target="_blank"
                    rel="noreferrer"
                    className="text-secondary hover:underline"
                  >
                    @SOUMIK7770
                  </a>
                </p>
                <p>Instagram / other socials can be added here later.</p>
              </div>
            </div>
          </motion.div>
        </section>

        {/* Feedback */}
        <section id="feedback" className="space-y-8">
          <motion.h2
            className="text-2xl md:text-3xl font-semibold"
            initial="hidden"
            whileInView="visible"
            viewport={{ once: true }}
            variants={fadeInUp}
          >
            Share Feedback or Ideas
          </motion.h2>

          <motion.div
            className="grid md:grid-cols-[1.1fr,0.9fr] gap-8"
            initial="hidden"
            whileInView="visible"
            viewport={{ once: true }}
            variants={fadeInUp}
            custom={0.1}
          >
            <form
              className="rounded-3xl bg-card border border-white/10 p-5 space-y-4 shadow-soft text-sm md:text-base text-slate-300"
              onSubmit={(e) => {
                e.preventDefault();
                alert(
                  "In production, this would send your feedback to Soumik. For now, connect via GitHub Issues!"
                );
              }}
            >
              <div className="grid md:grid-cols-2 gap-4">
                <div className="space-y-1">
                  <label className="text-xs text-slate-400">
                    Name (optional)
                  </label>
                  <input
                    type="text"
                    className="w-full rounded-xl bg-slate-900/70 border border-white/10 px-3 py-2 text-xs sm:text-sm focus:outline-none focus:ring-1 focus:ring-primary/60"
                    placeholder="Your name"
                  />
                </div>
                <div className="space-y-1">
                  <label className="text-xs text-slate-400">
                    Email (optional)
                  </label>
                  <input
                    type="email"
                    className="w-full rounded-xl bg-slate-900/70 border border-white/10 px-3 py-2 text-xs sm:text-sm focus:outline-none focus:ring-1 focus:ring-primary/60"
                    placeholder="you@example.com"
                  />
                </div>
              </div>

              <div className="space-y-1">
                <label className="text-xs text-slate-400">Message</label>
                <textarea
                  rows={4}
                  className="w-full rounded-xl bg-slate-900/70 border border-white/10 px-3 py-2 text-xs sm:text-sm focus:outline-none focus:ring-1 focus:ring-primary/60 resize-none"
                  placeholder="Share your feedback, ideas, or issues here..."
                />
              </div>

              <div className="flex items-center justify-between gap-3 flex-wrap">
                <button
                  type="submit"
                  className="px-5 py-2.5 rounded-xl bg-gradient-to-r from-primary to-secondary text-xs sm:text-sm font-medium shadow-soft hover:scale-[1.02] active:scale-[0.99] transition"
                >
                  Submit Feedback
                </button>
                <p className="text-[11px] text-slate-500">
                  Later you can connect this form to Firebase, Google Forms, or
                  a custom backend.
                </p>
              </div>
            </form>

            <div className="rounded-3xl bg-card border border-white/10 p-5 space-y-4 shadow-soft text-xs sm:text-sm text-slate-300">
              <p className="font-semibold text-slate-100">
                Roadmap & Future ideas
              </p>
              <ul className="space-y-1 list-disc list-inside text-slate-400">
                <li>Firefox & Edge extension packaging</li>
                <li>One-click installer with auto-updates</li>
                <li>Support for more college Wi-Fi portals</li>
                <li>GUI to safely update credentials</li>
              </ul>
              <p className="text-[11px] text-slate-500">
                If you&apos;d like to collaborate, fork the repo and open a PR —
                or just ping me with your idea.
              </p>
            </div>
          </motion.div>
        </section>
      </main>

      {/* Footer */}
      <footer className="border-t border-white/5 bg-slate-950/70">
        <div className="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8 py-4 flex flex-col md:flex-row items-center justify-between gap-2 text-[11px] sm:text-xs text-slate-500">
          <p>
            © {new Date().getFullYear()} LPU AutoLogin+ • Built by{" "}
            <span className="text-slate-300">SOUMIK POHI</span>
          </p>
          <p>Made with React, Tailwind CSS &amp; Framer Motion.</p>
        </div>
      </footer>
    </div>
  );
}

function SocialChip({ label, href }) {
  return (
    <a
      href={href}
      target="_blank"
      rel="noreferrer"
      className="px-3 py-1.5 rounded-full bg-white/5 border border-white/10 text-[11px] sm:text-xs text-slate-200 hover:bg-white/10 transition"
    >
      {label}
    </a>
  );
}

function Pill({ label }) {
  return (
    <span className="px-3 py-1 rounded-full bg-white/5 border border-white/10 text-[11px] sm:text-xs text-slate-300">
      {label}
    </span>
  );
}

export default App;


